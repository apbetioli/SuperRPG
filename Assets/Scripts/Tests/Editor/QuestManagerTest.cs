using System;
using System.Collections.Generic;
using System.Threading;
using NUnit.Framework;
using UnityEngine;

[TestFixture]
public class QuestManagerTest {

	[Test]
	public void StartQuest() {
//		EnemyManager enemyManager = new EnemyManager (typeof(Rat), 2);
//		Character player = new Character ();
//		player.weapon = new WoodenSword ();
//		QuestManager manager= new QuestManager (enemyManager, player);
//
//		Assert.AreEqual (2, enemyManager.GetEnemies().Count);
//		Assert.IsNotNull (enemyManager.GetNextEnemy());
//
//		Assert.AreEqual (10, enemyManager.GetNextEnemy().GetHealth ());
//		Assert.AreEqual (100, player.GetHealth ());
//
//		manager.StartQuest (0);
//
//		Assert.AreEqual (5, enemyManager.GetNextEnemy().GetHealth ());
//		Assert.AreEqual (98, player.GetHealth ());
	}

}
